// App.tsx — your starting point. Edit and `carbon dev` will hot-reload
// while preserving Solid signal values.
//
// Plain TSX with an explicit mount call. Works in every editor with
// zero setup. If you'd rather use the `@CarbonApp` directive instead,
// scaffold with `carbon init <name> --preset native`.

import { mount } from "@carbon/mini-solid";

function App() {
  return (
    <view class="app">
      
    </view>
  );
}

mount(() => <App />);
