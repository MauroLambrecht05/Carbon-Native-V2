# GENERATED — do not hand-edit. Source of truth is the `CFG`
# config in src/main.zig; run `zig build gen-manifest` after
# changing it. See manifest.zig's `toToml` doc comment for why
# this file exists at all alongside that config.
name = "file-search"
version = "0.1.0"
language = "zig"

extension-points = ["lifecycle.register","lifecycle.after_reload"]
modules = ["carbon:file-search"]

[exports."carbon:file-search"]
names = ["grep","glob","search","listSubdirs"]

[abi]
major = 1
minor = 6

[capabilities]
required = []
optional = []
