# versioning

Compatibility descriptors: what a build claims to interoperate with.

**Agreement** wire (`schema/versioning.fbs`) · **Honoured by** every tier
**Breaking it** is a wire break in the mechanism that detects wire breaks,
which is why this subject is append-only in practice.
