{
  "//": "Bun resolves tsconfig paths from the tsconfig nearest the IMPORTING file, not from the entrypoint's. Verified: with a tsconfig only under products/, a file in solutions/ that imports @carbon/* fails to resolve. So this tree needs its own.",
  "//options": "The compiler options are repeated from products/carbon-cli/tsconfig.json rather than inherited, because .config/tsconfig.base.json carries only the path aliases — it is the wiring both trees share, not a compiler profile. Without allowImportingTsExtensions the whole tree reports 'an import path can only end with .ts', since every internal import is explicit about the extension.",
  "extends": "../.config/tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "allowImportingTsExtensions": true,
    "esModuleInterop": true,
    "types": ["bun"]
  },
  "//include": "A single recursive glob, not a list of top-level folders. The folder list went stale the moment the tiers were renamed, and a stale include reports 'No inputs were found' pointing at line 1 — which reads like a syntax error and is not. This form cannot go stale while any .ts exists.",
  "include": ["**/*.ts"],
  "//exclude": "interface/renderer/ holds the Solid and React renderers, moved out of V1's carbon/runtime/engine/paint/renderers/ in migration phase 3. They are source in the right place and nothing depends on them yet: their npm dependencies (solid-js, react, react-reconciler) are not installed, so including them here is 9 'cannot find module' errors for code that is not being built. Phase 6 installs those, adds the tsconfig paths and the Bazel targets, and this entry comes out. See products/carbon/MIGRATION.md.",
  "//exclude-dom": "These need compiler options the rest of the tree must NOT have: lib DOM (xterm and three name browser types in their public APIs) and --jsx (three-fiber, and the two renderers). Letting DOM into this project would make a stray `document.` compile everywhere and fail at runtime. Each carries its own tsconfig and is checked separately — see .tools/validation/check_typescript.py.",
  "exclude": [
    "node_modules",
    "interface/renderer",
    "interface/stdlib/bindings",
    "integrations/terminal/xterm",
    "integrations/scene3d/three",
    "integrations/scene3d/three-fiber"
  ]
}
