# Carbon Native V2 - @carbon/three-fiber
#
# Integration: react-three-fiber's declarative surface, over Solid.

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "srcs",
    srcs = glob(["**/*.ts", "**/*.tsx", "**/*.d.ts", "package.json"], allow_empty = True),
)
