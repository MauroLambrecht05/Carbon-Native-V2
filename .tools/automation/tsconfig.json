{
  "//": "Bun resolves @carbon/* aliases from the tsconfig nearest the IMPORTING file, not from the entrypoint's. automation/testing imports @carbon/workspace, so this tree needs its own — the same reason products/ and solutions/ each have one. Documented in .config/tsconfig.base.json.",
  "//location": "Beside the code it configures, not at the .tools/ root. It was at .tools/tsconfig.json, which claimed the whole tier as one TypeScript project — but orchestration/ is Starlark, generators/ and validation/ are Python, and its own `include` said `automation/**/*.ts`. The include was already the truth; the file was in the wrong place.",
  "extends": "../../.config/tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "allowImportingTsExtensions": true,
    "esModuleInterop": true,
    "types": ["bun"]
  },
  "//exclude-benchmarks": "benchmarks/ is excluded: the scripts import msgpackr (not a dependency of this workspace) and reach into V1's ecosystem/ tree by relative path. They are measurement harnesses rather than shipped code, and rewiring them is separate work — tracked in products/carbon/MIGRATION.md rather than silently deleted.",
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "benchmarks"]
}
