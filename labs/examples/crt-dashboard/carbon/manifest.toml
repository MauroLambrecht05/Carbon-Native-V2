# carbon/manifest.toml — the real source of truth for which plugins compose
# this app, committed to version control. GENERATED: `carbon plugin new`/
# `add`/`enable`/`disable` are its only writers — do not hand-edit in normal
# use, the same posture as a package-lock.json, not a package.json.
#
# See solutions/capabilities/plugin/lifecycle/domain/services/AppManifestSection.ts
# and solutions/contracts/plugin/README.md.
schema = 1

[plugins.carbon-crt]
source = "local"
enabled = true
